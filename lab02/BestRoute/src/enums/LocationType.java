package enums;

/**
 * Represents the various types of locations available in the routing system.
 */
public enum LocationType {
    /** A city with a population. */
    City,
    /** An airport with terminals. */
    Airport,
    /** A gas station with specific gas prices. */
    GasStation
}